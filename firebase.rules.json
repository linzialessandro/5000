{
    "rules": {
        ".read": false,
        ".write": false,
        "rooms": {
            "$roomId": {
                ".read": "auth != null && (!data.child('expiresAt').exists() || data.child('expiresAt').val() > now) && (data.child('players').child(auth.uid).exists() || data.child('status').val() === 'waiting')",
                ".write": "auth != null && $roomId.length >= 4 && $roomId.length <= 10 && (!data.exists() || !data.child('expiresAt').exists() || data.child('expiresAt').val() > now) && ((!data.exists() && newData.exists() && newData.child('hostUid').val() === auth.uid && newData.child('players').child(auth.uid).exists() && newData.child('status').val() === 'waiting') || (data.exists() && newData.exists() && data.child('players').child(auth.uid).exists()) || (data.exists() && newData.exists() && data.child('status').val() === 'waiting' && newData.child('status').val() === 'waiting' && newData.child('players').child(auth.uid).exists() && !newData.child('playerOrder').child('6').exists() && newData.child('passHash').val() === data.child('passHash').val() && newData.child('hostUid').val() === data.child('hostUid').val()))",
                "hostUid": {
                    ".validate": "newData.isString() && (!data.exists() || newData.val() === data.val())"
                },
                "passHash": {
                    ".validate": "newData.isString() && newData.val().length === 64 && (!data.exists() || newData.val() === data.val())"
                },
                "status": {
                    ".validate": "newData.val() === 'waiting' || newData.val() === 'playing' || newData.val() === 'finished'"
                }
            }
        }
    }
}
